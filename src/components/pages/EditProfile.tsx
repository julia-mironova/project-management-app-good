import { Container } from '@mui/material';

const EditProfile = () => {
  return (
    <Container maxWidth="lg" sx={{ mt: '1rem', pb: 3, height: 'calc(100vh - 148px)' }}>
      EditProfile
    </Container>
  );
};

export default EditProfile;
